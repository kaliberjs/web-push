# Test Data

## `vapid.json`

The `vapid.json` file contains a set of VAPID keys that are used for testing purposes only. These keys are not production secrets and should not be used in a production environment.
